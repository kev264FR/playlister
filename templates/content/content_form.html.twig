
<h1>Ajouter du contenu</h1>
<hr>

{{form_start(form, {attr: {id: "content_form"}})}}

    {{form_row(form.title)}}
    {{form_row(form.url)}}

<input type="submit" onclick="submitContent(event)" value="OK">
{{form_end(form)}}

