{% extends 'base.html.twig' %}

{% block body %}

<h1 class="text-center">
    Page temporaire de commentaire
</h1>

<hr>

{{form_start(form)}}
    {{form_row(form.text)}}
    <input class="btn btn-success" type="submit" value="Commenté">
{{form_end(form)}}

{% endblock %}